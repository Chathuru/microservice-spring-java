name: Go Test

on:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: aws-ec2
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: mvn clean install
